# CanvasFlow 视觉规范

- 颜色：画布背景 `#f7f9fb`，正文 `#20252b`，边框 `#dce1e6`，主操作 `#2463eb`，校验错误 `#93434b`。
- 节点使用左侧色条区分类型；连接线、选中态和运行态不能只用颜色表达。
- 工具栏 54px，面板宽度 244px，节点圆角 6px，基础间距 4px。
- 状态覆盖尚未运行、排队中、执行成功、配置错误、保存失败和版本冲突。
- 所有图标按钮提供中文 tooltip；键盘焦点使用清晰描边。
